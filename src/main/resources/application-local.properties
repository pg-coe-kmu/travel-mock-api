## Local Profile Configuration
## The local profile may load the .env file for convenience (LOCAL_DATA_PATH,
## optional Supabase credentials for local S3 testing). The remote profile
## never reads .env - it uses real environment variables (CI secrets) only.
spring.config.import=optional:file:.env[.properties]
